Operations staff currently rely on the Customer Setup UI to update the SSN Indicator, but the UI does not display any validation errors when the update fails. As a result, users assume the change was successful even though the system silently ignores the update. This leads to incorrect downstream behavior, inconsistent processing, and a higher risk of human error.
